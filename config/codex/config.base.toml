# Non-secret Codex preferences. Merged into $CODEX_HOME/config.toml by
# bootstrap. Authentication (auth.json) and per-project trust levels are
# runtime state and are never written from here.
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
