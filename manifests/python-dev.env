# Pinned tool versions for the reusable python-dev environment.
#
# Python itself is intentionally NOT pinned here. Each repository owns its
# Python requirement through pyproject.toml and/or .python-version, and uv
# installs that interpreter into the isolated container HOME.

# Latest verified uv release when this environment was added.
UV_VERSION=0.12.10
