# Distribution packages required inside the dotnet-dev container.
#
# This file is the source of truth. distrobox/dotnet-dev.ini repeats the list
# because the INI format cannot read a file. bootstrap/dotnet-dev.sh converges
# an existing container from this manifest, and verify/29-dotnet-dev.sh checks it.

git
jq
gh
curl
# The distribution SDK. It is feature band 1xx only, so it cannot build a
# repository whose global.json pins another band. bootstrap/dotnet-dev.sh
# installs the pinned upstream SDK and puts it in front; this package stays as
# the fallback the container starts with. manifests/dotnet-sdk.env says why.
dotnet-sdk-10.0
gcc
gcc-c++
make
pkgconf-pkg-config
