# Domain docs

This repository uses one context. Read `CONTEXT.md` and the relevant records in
`docs/adr/` before work that uses project terms or decisions.

Create these files only when the first term needs definition or the first
decision needs a record. Keep `CONTEXT.md` as a glossary. Do not use it as a
specification or scratchpad.
